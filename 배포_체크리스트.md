# 다듬 스튜디오 — 배포 & 애드센스 체크리스트

> 목표: **트래픽 확보 → 구글 애드센스 승인 → 수익화**
> 최종 갱신: 2026-07-27

---

## ✅ 완료된 작업 (코드 쪽)

- [x] `package.json` 생성 (배포 플랫폼이 인식하는 시작 스크립트 `npm start`)
- [x] `.gitignore` 생성, `server.log` 삭제
- [x] `.env` 방식 도입 (`.env.example` 참고) — API 키를 코드에서 분리
- [x] **국어사전 → 국립국어원 표준국어대사전 공식 API로 교체** (스크레이핑 제거)
- [x] 국립국어원 출처 표기 추가 (이용약관 요구사항)
- [x] API 요청 제한(IP당 분당 40회) + 검사 글자 수 상한(5만 자)
- [x] `ALLOWED_ORIGIN` 으로 외부 무단 사용 차단 옵션
- [x] SEO 메타태그 / Open Graph / 트위터 카드 / canonical
- [x] 구조화 데이터(JSON-LD): `WebApplication` + `FAQPage`
- [x] `<h1>` 태그 지정
- [x] `robots.txt`, `sitemap.xml` 생성
- [x] 메인 페이지 SEO 콘텐츠 영역 (기능 소개 / 자주 틀리는 맞춤법 / FAQ)
- [x] 푸터 + 필수 페이지 4종 (`about` / `privacy` / `terms` / `contact`)

---

## 🔴 지금 바로 해야 할 일 (내 작업)

### 1. 국어사전 인증키 발급 — 10분
1. https://stdict.korean.go.kr/openapi/openApiInfo.do 접속 → 회원가입
2. "오픈 API 신청" → 인증키 즉시 발급 (무료)
3. `.env.example` 파일을 복사해 이름을 **`.env`** 로 변경
4. `STDICT_KEY=발급받은키` 입력 후 저장
5. `npm start` 로 실행 → 콘솔에 `공식 API 연결됨 ✅` 확인

### 2. 도메인 구매 — 30분, 연 1~2만원
- 가비아 / 후이즈 / Cloudflare Registrar 등
- 추천 형태: `dadeum.kr`, `matchumbeop.com` 처럼 짧고 기억하기 쉬운 것
- **애드센스는 자체 도메인이 승인률이 훨씬 높습니다**

### 3. 도메인 반영 — 15분
구매한 도메인을 다음 파일에서 `YOUR-DOMAIN.com` → 실제 도메인으로 바꾸기:
| 파일 | 위치 |
|---|---|
| `index.html` | canonical, og:url, og:image, twitter:image, JSON-LD 안 url (총 6곳) |
| `about.html` / `privacy.html` / `terms.html` / `contact.html` | canonical 각 1곳 |
| `robots.txt` | Sitemap 줄 |
| `sitemap.xml` | 모든 `<loc>` |

### 4. 내 정보 채우기 — 10분
- `privacy.html` — `[운영자 이메일]` 교체, 상단 ⚠️ 안내 상자 삭제
- `contact.html` — `[운영자 이메일]` 교체, 상단 ⚠️ 안내 상자 삭제
- 개인 메일 노출이 싫으면 이 사이트 전용 주소를 새로 만드는 걸 권장

### 5. 배포 — 1시간
1. GitHub 계정 생성 → 새 저장소(Repository) 만들기
2. 이 폴더를 저장소에 올리기 (`.env` 는 자동 제외됨)
3. https://render.com 가입 → New → Web Service → 저장소 연결
   - Build Command: (비움)
   - Start Command: `npm start`
4. Environment 탭에서 환경변수 등록:
   - `STDICT_KEY` = 발급받은 키
   - `ALLOWED_ORIGIN` = `https://내도메인.com`
5. Settings → Custom Domain 에서 구매한 도메인 연결

### 6. 구글 도구 연결 — 30분
- [ ] [Google Search Console](https://search.google.com/search-console) 등록 → 사이트맵 제출
- [ ] [Google Analytics 4](https://analytics.google.com) 등록 → 추적 코드 삽입
- [ ] 네이버 [서치어드바이저](https://searchadvisor.naver.com) 등록 (한국 트래픽에 중요)

---

## 🟡 애드센스 신청 전 필수 (3~6주)

### 7. 맞춤법 검사 엔진 교체 ⚠️ 가장 중요
현재 Daum 스크레이핑은 **이미 403 차단**된 상태이고, 애드센스 정책
("스크래핑된 콘텐츠") 위반이라 승인 후에도 계정 정지 위험이 있습니다.
→ 자체 규칙 엔진 고도화 또는 LLM API 기반 교정으로 교체 필요.
자세한 내용은 `API_연동_안내.txt` 참고.

### 8. 가이드 글 20~30개 작성 ⚠️ 승인의 실질적 관문
도구 페이지 하나만으로는 **"가치 있는 콘텐츠 없음"으로 거의 확실히 거절**됩니다.
글 하나당 1,500자 이상, 직접 작성 (AI 대량 생성 티가 나면 거절).

작성 예시 (그대로 검색 유입 키워드가 됩니다):
```
/guide/doe-dwae.html        되 vs 돼, 이것만 기억하세요
/guide/an-anh.html          '안'과 '않' 완벽 구분법
/guide/spacing-basics.html  한글 띄어쓰기 기본 규칙 10가지
/guide/myeochil.html        몇 일? 며칠? 헷갈리는 날짜 표기
/guide/waenji.html          왠지 vs 웬지, 웬일 vs 왠일
/guide/resume.html          자기소개서에서 가장 많이 틀리는 맞춤법 20선
/guide/report-tone.html     보고서 개조식 문체 작성법
/guide/email-manner.html    비즈니스 이메일 문장 다듬는 법
...
```
글을 추가할 때마다 `sitemap.xml` 에 `<url>` 항목을 추가하세요.

### 9. 자연 트래픽 누적 (최소 3~4주)
- 색인이 잡히고 방문자가 생겨야 "살아있는 사이트"로 평가됩니다
- 목표: 일 방문 50~100명 이상
- 홍보 채널: 네이버 블로그/카페, 에브리타임(대학생), 클리앙·뽐뿌, 브런치

---

## 🟢 애드센스 신청 (7주차~)

- [ ] https://adsense.google.com 가입 → 사이트 등록
- [ ] 발급받은 `ads.txt` 파일을 프로젝트 루트에 추가
- [ ] 심사 대기 (보통 2일~2주, 최대 1개월)
- [ ] **거절되면 사유 확인 후 보완해 재신청** — 재신청 횟수 제한 없음, 첫 거절은 흔한 일

### 승인 후 광고 배치
- ✅ 헤더 아래 가로 배너 / SEO 콘텐츠 영역 사이 / 가이드 글 본문 중간
- ❌ 검사 결과 위 오버레이, 버튼 바로 옆 → **무효 클릭으로 계정 정지**
- 자동 광고는 처음엔 끄고 수동 배치 권장 (도구 사용성 보호)

---

## 📅 시즌 전략
| 시기 | 수요 | 준비 시점 |
|---|---|---|
| 3월 · 9월 | 대학 과제·레포트 | 1~2개월 전 |
| 9~11월 | **공채 자기소개서 (최대 성수기)** | 7~8월 |
| 12~2월 | 논문·졸업 시즌 | 10~11월 |

관련 콘텐츠를 성수기 1~2개월 전에 올려두어야 색인이 잡혀 제때 유입됩니다.
